---
const { type = "container", className = "", style } = Astro.props;
---

<section
  class={`${
    type === "container" ? "container-1360-wrp" : ""
  } plr-15 ${className}`}
  style={style}
>
  <slot />
</section>
